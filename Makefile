install:
	sudo apt-get update
	sudo apt-get install -yy build-essential
	sudo apt-get install -yy graphicsmagick
	sudo apt-get install -yy poppler-utils poppler-data
	sudo apt-get install -yy ghostscript
	sudo apt-get install -yy pdftk
	sudo apt-get install -yy libreoffice
